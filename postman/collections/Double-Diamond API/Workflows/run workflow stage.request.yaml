$kind: http-request
method: POST
url: '{{baseUrl}}/workflows/:workflow_id/stages/:stage/run'
order: 3000
headers:
  - key: Content-Type
    value: application/json
pathVariables:
  - key: workflow_id
    value: 'workflow-123'
  - key: stage
    value: 'draft'
body:
  type: json
  content: |-
    {
      "input": {
        "prompt": "Summarize the project goals"
      }
    }